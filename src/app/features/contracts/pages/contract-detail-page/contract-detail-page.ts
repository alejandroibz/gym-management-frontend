import {CommonModule} from '@angular/common';import {ChangeDetectionStrategy,Component,inject,signal} from '@angular/core';import {MatButtonModule} from '@angular/material/button';import {MatIconModule} from '@angular/material/icon';import {ActivatedRoute,RouterLink} from '@angular/router';import {ClientContract} from '../../models/contract.model';import {ContractsService} from '../../services/contracts.service';
@Component({selector:'app-contract-detail-page',standalone:true,imports:[CommonModule,RouterLink,MatButtonModule,MatIconModule],templateUrl:'./contract-detail-page.html',styleUrl:'./contract-detail-page.scss',changeDetection:ChangeDetectionStrategy.OnPush})
export class ContractDetailPageComponent{readonly service=inject(ContractsService);private readonly route=inject(ActivatedRoute);readonly item=signal<ClientContract|null>(null);readonly error=signal('');constructor(){this.service.get(Number(this.route.snapshot.paramMap.get('id'))).subscribe({next:value=>this.item.set(value),error:()=>this.error.set('No se pudo cargar el contrato.')})}download(url:string,name:string):void{this.service.download(url).subscribe(blob=>{const objectUrl=URL.createObjectURL(blob),link=document.createElement('a');link.href=objectUrl;link.download=name;link.click();URL.revokeObjectURL(objectUrl)})}eventLabel(type:string):string{return({Issued:'Emitido',Viewed:'Visualizado',Signed:'Firmado',Downloaded:'Descargado',Voided:'Anulado',EmailDelivered:'Email entregado',EmailDeliveryFailed:'Falló el email'}as Record<string,string>)[type]??type}}
